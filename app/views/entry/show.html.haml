%h2= @entry.title
%div
  = "作成日:" + @entry.original_created_at.to_s
  = ", 更新日:" + @entry.original_updated_at.to_s

%div
  = "[" + @entry.tags.inject(""){|str,t| str += t.name + ", " } + "]"
%br
%div.body
  =raw @entry.content
